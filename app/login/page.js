
import LoginLayout from "./layout";

export const metadata = {
  title: 'manipesto.barrio | Login',
  description: '...',
}

function LoginMain(props) {
    return (
        <LoginLayout></LoginLayout>
    );
}

export default LoginMain;